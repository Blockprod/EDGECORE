# EDGECORE - Quick Start Guide

Bienvenue dans EDGECORE, un syst├¿me de trading quantitatif production-grade bas├® sur l'arbitrage statistique.

## ­ƒÜÇ Installation Express (3 minutes)

```powershell
# 1. Naviguer au projet
cd c:\Users\averr\EDGECORE

# 2. Cr├®er un environnement virtuel
python -m venv venv
venv\Scripts\activate

# 3. Installer les d├®pendances
pip install -r requirements.txt

# 4. V├®rifier la configuration
python check_health.py

# 5. Copier le template .env
copy .env.example .env

# 6. ├ëditer .env avec vos credentials (API IBKR, etc.)
# Notepad .env
```

## ­ƒôè Premi├¿re Session

### A. D├®couvrir des paires cointegrated

```powershell
python examples_pair_discovery.py
```

Cela va :
- Charger 2 ans de donn├®es historiques (AAPL, MSFT, JPM, etc.)
- Tester la cointegration Engle-Granger
- Afficher les paires candidates pour le trading

### B. Lancer un backtest

```powershell
python examples_backtest.py
```

Cela va :
- Charger les donn├®es historiques
- G├®n├®rer des signaux de trading bas├®s sur les Z-scores
- Calculer les m├®triques de performance (Sharpe, Drawdown, etc.)

### C. Paper Trading (mode sandbox)

```powershell
python main.py --mode paper --symbols AAPL/SPY MSFT/SPY
```

Cela va :
- Connecter ├á l'broker (en mode sandbox)
- G├®n├®rer des signaux en temps r├®el
- Placer des ordres de test
- Journaliser tous les ├®v├®nements

## ­ƒôü Structure Cl├®s

| Dossier | R├┤le |
|---------|------|
| `strategies/` | Logique de trading |
| `risk/` | Gestion du risque (IND├ëPENDANTE) |
| `execution/` | Abstraction broker (IBKR API, IBKR) |
| `backtests/` | Tests de performance |
| `monitoring/` | Logging structur├® |
| `config/` | Configuration YAML |

## ÔÜÖ´©Å Configuration

**Fichiers principaux:**
- `config/dev.yaml` ÔåÆ Environnement d├®veloppement (sandbox)
- `config/prod.yaml` ÔåÆ Production (live trading)

**Param├¿tres cl├®s ├á ajuster:**
```yaml
strategy:
  entry_z_score: 2.0      # Seuil d'entr├®e (├®cart-type)
  exit_z_score: 0.0       # Seuil de sortie (mean reversion)

risk:
  max_risk_per_trade: 0.005    # 0.5% du capital par trade
  max_concurrent_positions: 10  # Max positions ouvertes
  max_consecutive_losses: 3     # Kill-switch apr├¿s 3 pertes
```

## ­ƒôØ Workflow Complet

### 1´©ÅÔâú D├®veloppement
```powershell
python check_health.py              # V├®rifier le setup
python examples_pair_discovery.py   # Trouver les paires
python examples_backtest.py         # Backtest historique
```

### 2´©ÅÔâú Paper Trading
```powershell
# ├ëditer config/dev.yaml avec use_sandbox: true
python main.py --mode paper --symbols AAPL/SPY MSFT/SPY
```

Monitor les logs :
```powershell
Get-Content logs/main_*.log -Wait | ConvertFrom-Json | Select-Object timestamp, event, message
```

### 3´©ÅÔâú Live Trading (D├ëBUTANT SEULEMENT!)
```powershell
# ├ëTAPES CRITIQUES:
# 1. Start with 0.1 AAPL maximum
# 2. Monitor risk metrics continuously
# 3. Keep kill-switches active
# 4. Review logs every 30 minutes

python main.py --mode live --symbols AAPL/SPY
```

## ­ƒöÆ S├®curit├®

### Ô£ô ├Ç faire
- Garder `.env` hors du contr├┤le de version (dans `.gitignore`)
- Charger les credentials depuis les variables d'environnement
- Utiliser le sandbox avant le live
- Tester avec 1% du capital d'abord

### Ô£ù ├Ç ne PAS faire
- Ne pas committer `.env` avec des secrets
- Ne pas tester en live sans backtest d'abord
- Ne pas augmenter la taille de position sans ralentir
- Ne pas ignorer les logs de risque

## ­ƒôè Monitoring

Tous les ├®v├®nements sont logg├®s en JSON structur├® dans `logs/` :

```json
{
  "event_type": "TRADE_ENTRY",
  "timestamp": "2024-01-15T14:30:45Z",
  "symbol_pair": "AAPL_MSFT",
  "z_score": 2.15,
  "position_size": 0.5,
  "entry_price": 42500.00
}
```

Consulter les logs :
```powershell
Get-Content logs/main_*.log | ConvertFrom-Json | ForEach-Object { $_.message }
```

## ­ƒôê M├®triques Attendues

Backtest typique (strat├®gie conservative, 2 ans) :
- **Sharpe Ratio**: 1.5 - 2.0
- **Max Drawdown**: 10% - 15%
- **Win Rate**: 55% - 65%
- **Profit Factor**: 1.3 - 1.8

*Note: R├®sultats live diff├®reront due au slippage, commissions, et changements de r├®gime.*

## ­ƒåÿ Troubleshooting

### ImportError: No module named 'numpy'
```powershell
pip install -r requirements.txt
```

### ConnectionError to IBKR
- V├®rifier la cl├® API dans `.env`
- V├®rifier `use_sandbox: true` en dev
- V├®rifier la connexion internet

### Pas de paires cointegrated trouv├®es
- Augmenter le nombre de symboles
- Allonger la p├®riode de lookback
- V├®rifier que les donn├®es sont ├á jour

## ­ƒôÜ Lectures Recommand├®es

- **Cointegration**: "Econometric Modeling" - Engle & Granger
- **Pair Trading**: "Pairs Trading" - Ganapathy Vidyamurthy
- **Risk Management**: "Expected Shortfall" - Basel III

## ­ƒÄ» Prochaines ├ëtapes

1. Ex├®cuter `check_health.py` pour v├®rifier le setup
2. D├®couvrir les paires avec `examples_pair_discovery.py`
3. Tester avec un backtest
4. D├®ployer en sandbox
5. Augmenter progressivement les positions

## ÔÜá´©Å Disclaimer

**Ce syst├¿me est fourni ├á titre ├®ducatif. Le trading implique un risque substantiel de perte.**

- Testez rigoureusement avant d'utiliser de l'argent r├®el
- Les performances pass├®es ne garantissent pas les r├®sultats futurs
- Respectez rigoureusement les r├¿gles de gestion du risque
- Consultez un advisor financier avant de trader en live

---

**Bon trading! ­ƒôê**
