root = true

[*]
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true

# Batch files MUST use CRLF or cmd.exe will silently fail
[*.bat]
end_of_line = crlf
charset = utf-8
